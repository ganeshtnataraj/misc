vnet and 3 subnets 
	1 for dbs
	1 for aks 
	1 for vms, using NSG only requried ports were open for communication

aks cluster was full private , this aks cluster was accesable using a bastain VM where had configured  SSH session recoding also 2FA was enabled

other than admin user rest all users had restrictions to run and execute commands only specific command were allowed to run - restricted bash setup

creating a non -root user in the dockerfile - this restricts the root level privilages 

securityContext:
  privileged: false
  allowPrivilegeEscalation: false

  privileged	Container	Give full host privileges (unsafe)

  allowPrivilegeEscalation	Container	Controls Linux privilege escalation


annotations:
    helm.sh/resource-policy: keep
    acme.cert-manager.io/http01-edit-in-place: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
    nginx.ingress.kubernetes.io/client-max-body-size: "0"
    kubernetes.io/ingress.allow-http: "false"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "15600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "15600"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "15600"
    nginx.ingress.kubernetes.io/use-regex: "false"
    nginx.ingress.kubernetes.io/server-snippet: |
      client_header_buffer_size 100k;
      large_client_header_buffers 2 100k;
    nginx.ingress.kubernetes.io/configuration-snippet: |
      add_header X-Frame-Options "DENY";
      add_header X-XSS-Protection "1;mode=block";
      add_header X-Content-Type-Options "nosniff";
      add_header Referrer-Policy "origin";


   Azure microsoft Defender :
   	Security Posture
   	Regulatory Complaince
   	Workload Protection
